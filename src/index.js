import fullName from "./formatted/fullName";
import formatPhoneNumber from "./formatted/phoneNumber";
import {isObject, isString, isArray, isNumber} from "./validation";
export default {
    fullName,
    formatPhoneNumber,
    isObject,
    isString,
    isArray,
    isNumber
}